RewriteEngine On
DirectoryIndex index.php

# =================================================
# IGNORA ARQUIVOS E PASTAS REAIS (assets, uploads)
# =================================================
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# =================================================
# TODAS AS ROTAS VIRTUAIS → index.php
# =================================================
RewriteRule ^ index.php [L]
<Files .env>
    Order allow,deny
    Deny from all
</Files>